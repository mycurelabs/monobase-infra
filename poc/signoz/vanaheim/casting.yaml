# SigNoz on-prem PoC (vanaheim) — deployed as Docker containers via foundryctl.
# Latest SigNoz, docker/compose flavor. See poc/signoz/README.md.
apiVersion: v1alpha1
kind: Installation
metadata:
  name: signoz
spec:
  deployment:
    flavor: compose
    mode: docker
