# MyCure Staging Configuration
# Staging environment on DigitalOcean Kubernetes (DOKS)

global:
  domain: stg.localfirsthealth.com
  namespace: mycure-staging
  environment: staging

  nodePool: "staging"

  gateway:
    name: nginx-shared-gateway
    namespace: nginx-gateway-system
    sectionName: https-stg-lfh # NGINX Gateway listener

  storage:
    provider: ""
    className: ""

# ===== CORE: Monobase API =====
api:
  enabled: false

  image:
    repository: ghcr.io/monobaselabs/api
    tag: "0.1.4"
    pullPolicy: IfNotPresent

  replicaCount: 1

  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 512Mi

  gateway:
    hostname: ""

  livenessProbe:
    enabled: true

  readinessProbe:
    enabled: true

  podDisruptionBudget:
    enabled: false

  autoscaling:
    enabled: false

  backup:
    enabled: false

  externalSecrets:
    enabled: false

  postgresql:
    serviceName: postgresql
    architecture: standalone
    auth:
      database: postgres
      username: postgres

  mailpit:
    enabled: true

# ===== CORE: Monobase Account Frontend =====
account:
  enabled: false

  image:
    repository: ghcr.io/monobaselabs/accountapp
    tag: "1.0.2"
    pullPolicy: IfNotPresent

  replicaCount: 1

  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 128Mi

  gateway:
    hostname: ""

  config:
    API_URL: "https://hapihub.stg.localfirsthealth.com"

  podDisruptionBudget:
    enabled: false

# ===== HEALTHCARE: HapiHub API =====
hapihub:
  enabled: true

  image:
    repository: ghcr.io/mycurelabs/hapihub
    tag: "11.2.9"
    pullPolicy: IfNotPresent

  replicaCount: 1

  resources:
    requests:
      cpu: 1000m
      memory: 768Mi
    limits:
      cpu: "2"
      memory: 2Gi

  gateway:
    hostname: ""
    sectionName: https-stg-lfh
    # timeouts removed: nginx-shared-gateway doesn't support
    # spec.rules[].timeouts and was logging UnsupportedField warnings.
    # The same effect is achieved via snippetsPolicy.proxyReadTimeout below.
    snippetsPolicy:
      enabled: true
      proxyReadTimeout: "3600s"
      clientMaxBodySize: "50m"

  betterAuth:
    passkey:
      rpName: "Mycure"
      rpId: "localfirsthealth.com"

  config:
    CORS_ORIGINS: "https://mycure.stg.localfirsthealth.com,https://mycurev8.stg.localfirsthealth.com,https://mycurelocal.stg.localfirsthealth.com,https://mycure-myaccount.stg.localfirsthealth.com,https://mycure-deploydash.stg.localfirsthealth.com,https://dentalemon.stg.localfirsthealth.com,https://dentalemon-myaccount.stg.localfirsthealth.com,https://dentalemon-website.stg.localfirsthealth.com,https://dentalemon.stg.mycure.md,https://mycure-pr--*.web.app"
    CORS_STRICT: "true"
    CORS_ALLOW_LOCAL_NETWORK: "false"
    CORS_ALLOW_MOBILE_APPS: "true"
    ACCOUNTS_SERVICE_ACCOUNT_EMAILS: "service@test.com"

  env:
    - name: BETTER_AUTH_RATE_LIMIT_ENABLED
      value: "true"
    - name: BETTER_AUTH_RATE_LIMIT_WINDOW
      value: "60"
    - name: BETTER_AUTH_RATE_LIMIT_MAX
      value: "300"
    - name: BETTER_AUTH_SESSION_COOKIE_CACHE_ENABLED
      value: "false"

  livenessProbe:
    enabled: true

  readinessProbe:
    enabled: true

  podDisruptionBudget:
    enabled: false

  autoscaling:
    enabled: false
    minReplicas: 1
    maxReplicas: 1

  externalSecrets:
    enabled: true
    secretStore: gcp-secretstore
    secretStoreKind: ClusterSecretStore
    refreshInterval: 1h
    secrets:
      - secretKey: GOOGLE_CLIENT_ID
        remoteKey: mycure-staging-google-oauth-client-id
      - secretKey: GOOGLE_CLIENT_SECRET
        remoteKey: mycure-staging-google-oauth-client-secret
      - secretKey: STRIPE_SECRET_KEY
        remoteKey: mycure-staging-stripe-secret-key
      - secretKey: AUTH_SECRET
        remoteKey: mycure-staging-auth-secret
      - secretKey: BETTER_AUTH_SECRET
        remoteKey: mycure-staging-better-auth-secret
      - secretKey: PRIVATE_KEY
        remoteKey: mycure-staging-private-key
      # Encryption keys (synced from production for data clone)
      - secretKey: ENC_BILLING_INVOICES
        remoteKey: mycure-staging-enc-billing-invoices
      - secretKey: ENC_BILLING_ITEMS
        remoteKey: mycure-staging-enc-billing-items
      - secretKey: ENC_BILLING_PAYMENTS
        remoteKey: mycure-staging-enc-billing-payments
      - secretKey: ENC_MEDICAL_RECORDS
        remoteKey: mycure-staging-enc-medical-records
      - secretKey: ENC_PERSONAL_DETAILS
        remoteKey: mycure-staging-enc-personal-details

  postgresql:
    enabled: true
    serviceName: postgresql
    auth:
      database: hapihub

  mongodb:
    serviceName: mongodb
    database: hapihub
    username: root
    replicaSet: rs0

# ===== HEALTHCARE: Frontend Application =====
mycure:
  enabled: true

  image:
    repository: ghcr.io/mycurelabs/mycureapp
    tag: "10.4.2"
    pullPolicy: IfNotPresent

  replicaCount: 1

  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 128Mi

  gateway:
    hostname: ""
    sectionName: https-stg-lfh

  config:
    API_URL: "https://hapihub.stg.localfirsthealth.com"
    HAPIHUB_URL: "https://hapihub.stg.localfirsthealth.com"

  podDisruptionBudget:
    enabled: false

# ===== HEALTHCARE: MyCure Local Frontend Application =====
mycurelocal:
  enabled: false

  image:
    repository: ghcr.io/mycurelabs/mycurelocal
    tag: "1.4.0"
    pullPolicy: IfNotPresent

  replicaCount: 1

  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 128Mi

  gateway:
    hostname: ""
    sectionName: https-stg-lfh

  podDisruptionBudget:
    enabled: false

# ===== HEALTHCARE: MyCure MyAccount Application =====
mycure-myaccount:
  enabled: true

  image:
    repository: ghcr.io/mycurelabs/myaccount
    tag: "1.0.1"
    pullPolicy: IfNotPresent

  replicaCount: 1

  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 128Mi

  gateway:
    hostname: ""
    sectionName: https-stg-lfh

  config:
    API_URL: "https://hapihub.stg.localfirsthealth.com"
    HAPIHUB_URL: "https://hapihub.stg.localfirsthealth.com"

  podDisruptionBudget:
    enabled: false

# ===== HEALTHCARE: DentaLemon MyAccount Application =====
dentalemon-myaccount:
  enabled: true

  image:
    repository: ghcr.io/mycurelabs/dentalemon-myaccount
    tag: "1.0.14"
    pullPolicy: IfNotPresent

  replicaCount: 1

  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 128Mi

  gateway:
    hostname: ""
    sectionName: https-stg-lfh

  config:
    API_URL: "https://hapihub.stg.localfirsthealth.com"
    HAPIHUB_URL: "https://hapihub.stg.localfirsthealth.com"

  podDisruptionBudget:
    enabled: false

# ===== HEALTHCARE: MyCure DeployDash Application =====
mycure-deploydash:
  enabled: true

  image:
    repository: ghcr.io/mycurelabs/deploydash
    tag: "10.0.5"
    pullPolicy: IfNotPresent

  replicaCount: 1

  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 128Mi

  gateway:
    hostname: ""
    sectionName: https-stg-lfh

  config:
    API_URL: "https://hapihub.stg.localfirsthealth.com"
    HAPIHUB_URL: "https://hapihub.stg.localfirsthealth.com"

  podDisruptionBudget:
    enabled: false

# ===== HEALTHCARE: Frontend Application (v8 Legacy) =====
mycurev8:
  enabled: true

  image:
    repository: ghcr.io/mycurelabs/mycureapp
    tag: "8.24.7"
    pullPolicy: IfNotPresent

  replicaCount: 1

  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 128Mi

  gateway:
    hostname: ""
    sectionName: https-stg-lfh

  config:
    API_URL: "https://hapihub.stg.localfirsthealth.com"
    HAPIHUB_URL: "https://hapihub.stg.localfirsthealth.com"
    MYCURE_X_URL: "https://mycure.stg.localfirsthealth.com"

  podDisruptionBudget:
    enabled: false

# ===== HEALTHCARE: HapiHub API Documentation =====
hapihub-docs:
  enabled: true

  image:
    repository: ghcr.io/mycurelabs/hapihub-docs
    tag: "1.1.0"
    pullPolicy: IfNotPresent

  replicaCount: 1

  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 128Mi

  gateway:
    hostname: "docs.stg.localfirsthealth.com"
    sectionName: https-stg-lfh

  podDisruptionBudget:
    enabled: false

# ===== HEALTHCARE: DentaLemon Website =====
# Routed through NGINX Gateway Fabric (staging proof-of-concept)
dentalemon-website:
  enabled: true

  image:
    repository: ghcr.io/mycurelabs/dentalemon-website
    tag: "0.3.1"
    pullPolicy: IfNotPresent

  replicaCount: 1

  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 128Mi

  gateway:
    hostnames:
      - hostname: dentalemon-website.stg.localfirsthealth.com
        sectionName: https-stg-lfh
      - hostname: dentalemon.stg.mycure.md
        sectionName: https-stg-mycure
    gatewayName: nginx-shared-gateway
    gatewayNamespace: nginx-gateway-system

  networkPolicy:
    enabled: true

  config:
    API_URL: "https://hapihub.stg.localfirsthealth.com"
    ACCOUNT_URL: "https://dentalemon-myaccount.stg.localfirsthealth.com"

  podDisruptionBudget:
    enabled: false

# ===== HEALTHCARE: DentaLemon Dental Application =====
dentalemon:
  enabled: true

  image:
    repository: ghcr.io/mycurelabs/dentalemonapp
    tag: "10.9.1"
    pullPolicy: IfNotPresent

  replicaCount: 1

  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 128Mi

  gateway:
    hostname: ""
    sectionName: https-stg-lfh

  config:
    API_URL: "https://hapihub.stg.localfirsthealth.com"
    HAPIHUB_URL: "https://hapihub.stg.localfirsthealth.com"

  podDisruptionBudget:
    enabled: false

# ===== HEALTHCARE: SyncD Real-time Synchronization =====
syncd:
  enabled: false

  image:
    repository: ghcr.io/mycurelabs/syncd
    tag: "5.130.36"
    pullPolicy: IfNotPresent

  replicaCount: 1

  resources:
    requests:
      cpu: 250m
      memory: 384Mi
    limits:
      cpu: "2"
      memory: 4Gi

  gateway:
    hostname: ""
    sectionName: https-stg-lfh

  livenessProbe:
    enabled: true

  readinessProbe:
    enabled: true

  podDisruptionBudget:
    enabled: false

  autoscaling:
    enabled: false

  externalSecrets:
    enabled: false

  mongodb:
    serviceName: mongodb
    database: hapihub
    username: root
    replicaSet: rs0

# ===== DATABASE: PostgreSQL =====
postgresql:
  enabled: true

  fullnameOverride: "postgresql"

  architecture: standalone

  image:
    repository: bitnamilegacy/postgresql
    tag: 16.4.0-debian-12-r13

  auth:
    enabled: true
    database: hapihub
    username: postgres
    existingSecret: postgresql
    secretKeys:
      adminPasswordKey: postgres-password

  persistence:
    enabled: true
    storageClass: ""
    size: 20Gi

  resources:
    requests:
      cpu: 500m
      memory: 4Gi
    limits:
      cpu: 2000m
      memory: 8Gi

  podDisruptionBudget:
    enabled: false

# ===== DATABASE: MongoDB =====
mongodb:
  enabled: false

  fullnameOverride: "mongodb"

  architecture: replicaset
  replicaCount: 1

  image:
    repository: bitnamilegacy/mongodb
    tag: 7.0.15-debian-12-r0

  auth:
    rootUser: root
    existingSecret: mongodb

  arbiter:
    enabled: false

  livenessProbe:
    enabled: true
    timeoutSeconds: 30
    periodSeconds: 30
    failureThreshold: 10

  readinessProbe:
    enabled: true
    timeoutSeconds: 20
    periodSeconds: 20
    failureThreshold: 10

  persistence:
    enabled: true
    storageClass: ""
    size: 20Gi

  resources:
    requests:
      cpu: 100m
      memory: 640Mi
    limits:
      cpu: 500m
      memory: 1Gi

  podDisruptionBudget:
    enabled: false

# ===== CACHE: Valkey (Redis) =====
valkey:
  enabled: false  # disabled 2026-04-07 (incident)

  global:
    security:
      allowInsecureImages: true

  auth:
    existingSecret: valkey

  architecture: standalone

  image:
    repository: bitnamilegacy/valkey
    tag: 8.0.1-debian-12-r2

  master:
    persistence:
      enabled: true
      storageClass: ""
      size: 4Gi
    # Bumped 2026-04-06: the primary OOM-kills during AOF/RDB load (RDB ≈ 376Mi
    # base with ~425k keys, plus AOF incr replay and cadence working set).
    # 512Mi was causing 277 restarts in 27h and a dead cadence metadata backend;
    # 1Gi was still not enough — valkey peaked during AOF replay and tripped
    # the same OOM. 2Gi gives real headroom. CPU bumped so the initial replay
    # doesn't starve the liveness probe.
    resources:
      requests:
        cpu: 100m
        memory: 1Gi
      limits:
        cpu: 500m
        memory: 2Gi

# ===== STORAGE: MinIO Object Storage =====
minio:
  enabled: true

  fullnameOverride: "minio"

  image:
    repository: bitnamilegacy/minio
    tag: "2025.7.23-debian-12-r3"

  mode: standalone

  statefulset:
    replicaCount: 1

  auth:
    existingSecret: minio

  persistence:
    enabled: true
    storageClass: ""
    size: 10Gi

  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 512Mi

  defaultBuckets: "monobase-files"

  buckets:
    - name: "monobase-files"
  region: "us-east-1"

  podSecurityContext:
    enabled: true
    fsGroup: 1001
    fsGroupChangePolicy: "OnRootMismatch"

  containerSecurityContext:
    enabled: true
    runAsUser: 1001
    runAsGroup: 1001
    runAsNonRoot: true
    privileged: false
    readOnlyRootFilesystem: true
    allowPrivilegeEscalation: false
    capabilities:
      drop:
        - ALL
    seccompProfile:
      type: RuntimeDefault

  gateway:
    enabled: true
    hostname: ""
    sectionName: https-stg-lfh
    rateLimit:
      enabled: false

# ===== EMAIL TESTING: Mailpit =====
mailpit:
  enabled: true

  resources:
    requests:
      cpu: 25m
      memory: 32Mi
    limits:
      cpu: 100m
      memory: 64Mi

  gateway:
    enabled: true
    hostname: ""
    sectionName: https-stg-lfh

# ===== P2P SYNC: Cadence =====
cadence:
  enabled: false  # disabled 2026-04-07 (incident)

  image:
    repository: ghcr.io/mycurelabs/cadence
    tag: "0.4.1"
    pullPolicy: Always

  replicaCount: 1

  # Bumped 2026-04-06: 0.4.0 OOMs at 2Gi during initial valkey state load
  # (~425k metadata keys + 110-table watermark cache + iroh quic stack).
  # Bumped 2026-04-07: 4Gi still OOMs at startup — the applier's primary-DB
  # catch-up scans all ~118 watched tables in parallel, several of which
  # (personal_details_history, medical_records_history, activity_logs)
  # return large result sets before LIMIT kicks in. 8Gi stabilizes the
  # cold start; revisit if further OOMs show up after a sync-backlog.
  resources:
    requests:
      cpu: 200m
      memory: 2Gi
    limits:
      cpu: "2"
      memory: 8Gi

  gateway:
    hostname: ""
    sectionName: https-stg-lfh
    snippetsPolicy:
      enabled: true
      proxyReadTimeout: "3600s"
      clientMaxBodySize: "10m"

  postgresql:
    enabled: true
    serviceName: postgresql
    auth:
      database: hapihub

  valkey:
    enabled: true
    serviceName: "valkey-primary"

  config:
    RUST_LOG: "info"

# ===== BACKUP =====
backup:
  enabled: false

# ===== SECURITY =====
podSecurityStandards:
  enabled: true
  level: restricted

# ===== RESOURCE QUOTAS =====
resourceQuotas:
  enabled: false
