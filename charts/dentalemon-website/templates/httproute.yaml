{{- if and .Values.enabled .Values.gateway.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "dentalemon-website.fullname" . }}
  namespace: {{ include "dentalemon-website.namespace" . }}
  labels:
    {{- include "dentalemon-website.labels" . | nindent 4 }}
spec:
  parentRefs:
    {{- if .Values.gateway.hostnames }}
    {{- range .Values.gateway.hostnames }}
    - name: {{ include "dentalemon-website.gateway.name" $ }}
      namespace: {{ include "dentalemon-website.gateway.namespace" $ }}
      sectionName: {{ .sectionName }}
    {{- end }}
    {{- else }}
    - name: {{ include "dentalemon-website.gateway.name" . }}
      namespace: {{ include "dentalemon-website.gateway.namespace" . }}
      {{- if .Values.gateway.sectionName }}
      sectionName: {{ .Values.gateway.sectionName }}
      {{- else }}
      sectionName: https  # Default: attach to HTTPS listener
      {{- end }}
    {{- end }}
  hostnames:
    {{- if .Values.gateway.hostnames }}
    {{- range .Values.gateway.hostnames }}
    - {{ .hostname }}
    {{- end }}
    {{- else }}
    - {{ include "dentalemon-website.gateway.hostname" . }}
    {{- end }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: {{ include "dentalemon-website.fullname" . }}
          port: {{ .Values.service.port }}
          weight: 1
{{- end }}
