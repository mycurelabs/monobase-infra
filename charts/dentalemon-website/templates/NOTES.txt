{{- if .Values.enabled }}
DentaLemon Website has been deployed!

Chart: {{ .Chart.Name }}-{{ .Chart.Version }}
Release: {{ .Release.Name }}
Namespace: {{ include "dentalemon-website.namespace" . }}

Deployment Information:
  - Replicas: {{ .Values.replicaCount }}
  - Image: {{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}
  - Service: {{ include "dentalemon-website.fullname" . }}:{{ .Values.service.port }}

Access Information:
{{- if .Values.gateway.enabled }}
  DentaLemon Website App: https://{{ include "dentalemon-website.gateway.hostname" . }}

  Open in browser:
  open https://{{ include "dentalemon-website.gateway.hostname" . }}
{{- else }}
  Gateway is disabled. Use port-forward to access:
  kubectl port-forward -n {{ include "dentalemon-website.namespace" . }} svc/{{ include "dentalemon-website.fullname" . }} 8080:{{ .Values.service.port }}
  # Open http://localhost:8080
{{- end }}

Next Steps:
  1. Verify pods are running:
     kubectl get pods -n {{ include "dentalemon-website.namespace" . }} -l app.kubernetes.io/name={{ include "dentalemon-website.name" . }}

  2. Check logs:
     kubectl logs -n {{ include "dentalemon-website.namespace" . }} -l app.kubernetes.io/name={{ include "dentalemon-website.name" . }} --tail=50

  3. Test frontend:
     curl https://{{ include "dentalemon-website.gateway.hostname" . }}

{{- else }}
DentaLemon Website is disabled (enabled: false)
{{- end }}
