{{- if .Values.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: poc-iroh
  namespace: {{ .Values.global.namespace }}
  labels:
    app.kubernetes.io/name: poc-iroh
spec:
  replicas: 1
  # RWO volume for the sqlite identity db → Recreate avoids attach deadlock on update.
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: poc-iroh
  template:
    metadata:
      labels:
        app.kubernetes.io/name: poc-iroh
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: poc-iroh
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop: ["ALL"]
        args: ["hub"]
        env:
        - name: CADENCE_POC_UDP_PORT
          value: {{ .Values.udpPort | quote }}
        - name: CADENCE_POC_DB
          value: /data/poc.db
        {{- if .Values.publicAddr }}
        # Hub advertises its own public LB address so peers reach it DIRECTLY by
        # EndpointId alone (no peer-side IP). A pod can't self-discover its LB IP.
        - name: CADENCE_POC_PUBLIC_ADDR
          value: {{ .Values.publicAddr | quote }}
        {{- end }}
        volumeMounts:
        - name: data
          mountPath: /data
        ports:
        - name: quic
          containerPort: {{ .Values.udpPort }}
          protocol: UDP
        - name: health
          containerPort: {{ .Values.udpPort }}
          protocol: TCP
        livenessProbe:
          tcpSocket:
            port: health
          initialDelaySeconds: 5
          periodSeconds: 15
        resources:
          {{- toYaml .Values.resources | nindent 10 }}
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: poc-iroh-data
      {{- if .Values.nodePool }}
      nodeSelector:
        node-pool: {{ .Values.nodePool }}
      tolerations:
      - key: "node-pool"
        operator: "Equal"
        value: {{ .Values.nodePool | quote }}
        effect: "NoSchedule"
      {{- end }}
{{- end }}
