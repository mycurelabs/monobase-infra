{{- if .Values.enabled }}
🎉 Dashboard Frontend has been deployed!

Chart: {{ .Chart.Name }}-{{ .Chart.Version }}
Release: {{ .Release.Name }}
Namespace: {{ include "mycure-dashboard.namespace" . }}

📋 Deployment Information:
  - Replicas: {{ .Values.replicaCount }}
  - Image: {{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}
  - Service: {{ include "mycure-dashboard.fullname" . }}:{{ .Values.service.port }}

🌐 Access Information:
{{- if .Values.gateway.enabled }}
  Dashboard App: https://{{ include "mycure-dashboard.gateway.hostname" . }}

  Open in browser:
  open https://{{ include "mycure-dashboard.gateway.hostname" . }}
{{- else }}
  Gateway is disabled. Use port-forward to access:
  kubectl port-forward -n {{ include "mycure-dashboard.namespace" . }} svc/{{ include "mycure-dashboard.fullname" . }} 8080:{{ .Values.service.port }}
  # Open http://localhost:8080
{{- end }}

🔍 Next Steps:
  1. Verify pods are running:
     kubectl get pods -n {{ include "mycure-dashboard.namespace" . }} -l app.kubernetes.io/name={{ include "mycure-dashboard.name" . }}

  2. Check logs:
     kubectl logs -n {{ include "mycure-dashboard.namespace" . }} -l app.kubernetes.io/name={{ include "mycure-dashboard.name" . }} --tail=50

  3. Test frontend:
     curl https://{{ include "mycure-dashboard.gateway.hostname" . }}

📱 Frontend Features:
  - Static file serving (nginx)
  - Read-only root filesystem (security)
  - CDN-ready configuration
  - Environment variable injection

{{- else }}
Dashboard Frontend is disabled (enabled: false)
{{- end }}
