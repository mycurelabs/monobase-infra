{{- if and .Values.enabled .Values.serviceMonitor.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "mycure-dashboard.fullname" . }}
  namespace: {{ include "mycure-dashboard.namespace" . }}
  labels:
    {{- include "mycure-dashboard.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "mycure-dashboard.selectorLabels" . | nindent 6 }}
  endpoints:
    - port: http
      path: {{ .Values.serviceMonitor.path | default "/metrics" }}
      interval: {{ .Values.serviceMonitor.interval | default "30s" }}
{{- end }}
