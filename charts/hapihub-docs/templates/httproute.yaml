{{- if and .Values.enabled .Values.gateway.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "hapihub-docs.fullname" . }}
  namespace: {{ include "hapihub-docs.namespace" . }}
  labels:
    {{- include "hapihub-docs.labels" . | nindent 4 }}
spec:
  parentRefs:
    - name: {{ include "hapihub-docs.gateway.name" . }}
      namespace: {{ include "hapihub-docs.gateway.namespace" . }}
      {{- if .Values.gateway.sectionName }}
      sectionName: {{ .Values.gateway.sectionName }}
      {{- else }}
      sectionName: https  # Default: attach to HTTPS listener
      {{- end }}
  hostnames:
    {{- if .Values.gateway.hostnames }}
    {{- range .Values.gateway.hostnames }}
    - {{ . }}
    {{- end }}
    {{- else }}
    - {{ include "hapihub-docs.gateway.hostname" . }}
    {{- end }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: {{ include "hapihub-docs.fullname" . }}
          port: {{ .Values.service.port }}
          weight: 1
{{- end }}
