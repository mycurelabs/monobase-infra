apiVersion: v2
name: tailscale-proxy
description: Unprivileged tailscale node that joins a tailnet and TCP-forwards :443 to one cluster Service (per-namespace tailnet exposure)
type: application
version: 1.0.0
appVersion: "v1.98.9"
home: https://github.com/mycurelabs/monobase-infra
sources:
  - https://github.com/tailscale/tailscale
