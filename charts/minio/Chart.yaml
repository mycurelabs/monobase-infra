apiVersion: v2
name: minio-wrapper
description: MinIO object storage with Gateway API integration (Bitnami wrapper)
type: application
version: 1.0.0
appVersion: "17.0.22"
keywords:
  - storage
  - s3
  - minio
  - object-storage
home: https://github.com/monobaselabs/monobase-infra
sources:
  - https://github.com/minio/minio
maintainers:
  - name: Your Organization
    email: devops@example.com

dependencies:
  - name: minio
    version: "17.0.22"
    repository: https://charts.bitnami.com/bitnami
    condition: enabled
