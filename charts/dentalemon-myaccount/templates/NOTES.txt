{{- if .Values.enabled }}
DentaLemon MyAccount has been deployed!

Chart: {{ .Chart.Name }}-{{ .Chart.Version }}
Release: {{ .Release.Name }}
Namespace: {{ include "dentalemon-myaccount.namespace" . }}

Deployment Information:
  - Replicas: {{ .Values.replicaCount }}
  - Image: {{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}
  - Service: {{ include "dentalemon-myaccount.fullname" . }}:{{ .Values.service.port }}

Access Information:
{{- if .Values.gateway.enabled }}
  DentaLemon MyAccount App: https://{{ include "dentalemon-myaccount.gateway.hostname" . }}

  Open in browser:
  open https://{{ include "dentalemon-myaccount.gateway.hostname" . }}
{{- else }}
  Gateway is disabled. Use port-forward to access:
  kubectl port-forward -n {{ include "dentalemon-myaccount.namespace" . }} svc/{{ include "dentalemon-myaccount.fullname" . }} 8080:{{ .Values.service.port }}
  # Open http://localhost:8080
{{- end }}

Next Steps:
  1. Verify pods are running:
     kubectl get pods -n {{ include "dentalemon-myaccount.namespace" . }} -l app.kubernetes.io/name={{ include "dentalemon-myaccount.name" . }}

  2. Check logs:
     kubectl logs -n {{ include "dentalemon-myaccount.namespace" . }} -l app.kubernetes.io/name={{ include "dentalemon-myaccount.name" . }} --tail=50

  3. Test frontend:
     curl https://{{ include "dentalemon-myaccount.gateway.hostname" . }}

{{- else }}
DentaLemon MyAccount is disabled (enabled: false)
{{- end }}
