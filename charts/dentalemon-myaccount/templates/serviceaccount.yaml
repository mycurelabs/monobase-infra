{{- if and .Values.enabled .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "dentalemon-myaccount.serviceAccountName" . }}
  namespace: {{ include "dentalemon-myaccount.namespace" . }}
  labels:
    {{- include "dentalemon-myaccount.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
