{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "mycure-myaccount.fullname" . }}
  namespace: {{ include "mycure-myaccount.namespace" . }}
  labels:
    {{- include "mycure-myaccount.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "mycure-myaccount.selectorLabels" . | nindent 4 }}
{{- end }}
