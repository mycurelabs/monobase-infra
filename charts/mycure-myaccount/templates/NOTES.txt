{{- if .Values.enabled }}
MyAccount has been deployed!

Chart: {{ .Chart.Name }}-{{ .Chart.Version }}
Release: {{ .Release.Name }}
Namespace: {{ include "mycure-myaccount.namespace" . }}

Deployment Information:
  - Replicas: {{ .Values.replicaCount }}
  - Image: {{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}
  - Service: {{ include "mycure-myaccount.fullname" . }}:{{ .Values.service.port }}

Access Information:
{{- if .Values.gateway.enabled }}
  MyAccount App: https://{{ include "mycure-myaccount.gateway.hostname" . }}

  Open in browser:
  open https://{{ include "mycure-myaccount.gateway.hostname" . }}
{{- else }}
  Gateway is disabled. Use port-forward to access:
  kubectl port-forward -n {{ include "mycure-myaccount.namespace" . }} svc/{{ include "mycure-myaccount.fullname" . }} 8080:{{ .Values.service.port }}
  # Open http://localhost:8080
{{- end }}

Next Steps:
  1. Verify pods are running:
     kubectl get pods -n {{ include "mycure-myaccount.namespace" . }} -l app.kubernetes.io/name={{ include "mycure-myaccount.name" . }}

  2. Check logs:
     kubectl logs -n {{ include "mycure-myaccount.namespace" . }} -l app.kubernetes.io/name={{ include "mycure-myaccount.name" . }} --tail=50

  3. Test frontend:
     curl https://{{ include "mycure-myaccount.gateway.hostname" . }}

{{- else }}
MyAccount is disabled (enabled: false)
{{- end }}
