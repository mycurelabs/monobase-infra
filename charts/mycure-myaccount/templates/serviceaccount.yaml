{{- if and .Values.enabled .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "mycure-myaccount.serviceAccountName" . }}
  namespace: {{ include "mycure-myaccount.namespace" . }}
  labels:
    {{- include "mycure-myaccount.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
