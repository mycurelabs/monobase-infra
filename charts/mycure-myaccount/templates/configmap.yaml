{{- if and .Values.enabled .Values.config }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mycure-myaccount.fullname" . }}
  namespace: {{ include "mycure-myaccount.namespace" . }}
  labels:
    {{- include "mycure-myaccount.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
{{- end }}
