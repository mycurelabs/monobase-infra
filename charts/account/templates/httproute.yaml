{{- if and .Values.enabled .Values.gateway.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "account.fullname" . }}
  namespace: {{ include "account.namespace" . }}
  labels:
    {{- include "account.labels" . | nindent 4 }}
spec:
  parentRefs:
    - name: {{ include "account.gateway.name" . }}
      namespace: {{ include "account.gateway.namespace" . }}
      sectionName: https  # Only attach to HTTPS listener
  hostnames:
    {{- if .Values.gateway.hostnames }}
    {{- range .Values.gateway.hostnames }}
    - {{ . }}
    {{- end }}
    {{- else }}
    - {{ include "account.gateway.hostname" . }}
    {{- end }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: {{ include "account.fullname" . }}
          port: {{ .Values.service.port }}
          weight: 1
{{- end }}
