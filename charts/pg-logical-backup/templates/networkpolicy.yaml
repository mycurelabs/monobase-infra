{{- if and .Values.enabled .Values.networkPolicy.enabled }}
# Egress-only: the backup pod initiates connections, accepts none.
#   - DNS (kube-dns)
#   - the in-cluster PG (primary for role provision, replica for the dump)
#   - destinations[]: the off-cloud upload target(s), destination-scoped
#     (specific CIDR + ports; NEVER 0.0.0.0/0) — pinned per-deployment.
#   - extraEgress: raw escape-hatch rules (rare).
#
# Fail-closed guard: if the backup is enabled but no upload destination is
# pinned (and no raw extraEgress), the pod could never reach niflheim — surface
# that at render time rather than shipping a policy that silently blocks every
# upload. (Set networkPolicy.enabled: false to opt out entirely.)
{{- if and (not .Values.networkPolicy.destinations) (not .Values.networkPolicy.extraEgress) }}
{{- fail "pg-logical-backup: networkPolicy is enabled but no upload destination is pinned. Set networkPolicy.destinations (e.g. niflheim tailnet /32 + port 22) in the deployment overlay, or networkPolicy.extraEgress, or disable the NetworkPolicy. See charts/pg-logical-backup/NOTES.txt." }}
{{- end }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "pgLogicalBackup.fullname" . }}
  namespace: {{ include "pgLogicalBackup.namespace" . }}
  labels:
    {{- include "pgLogicalBackup.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "pgLogicalBackup.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Egress
  egress:
    # DNS
    - to:
        - namespaceSelector: {}
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    # In-cluster PostgreSQL (primary for role provision, replica for the dump)
    - to:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: postgresql
      ports:
        - protocol: TCP
          port: {{ .Values.database.port }}
    {{- range .Values.networkPolicy.destinations }}
    # Off-cluster upload destination (pinned, destination-scoped)
    - to:
        - ipBlock:
            cidr: {{ required "networkPolicy.destinations[].cidr is required" .cidr }}
      {{- with .ports }}
      ports:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    {{- end }}
    {{- with .Values.networkPolicy.extraEgress }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
{{- end }}
