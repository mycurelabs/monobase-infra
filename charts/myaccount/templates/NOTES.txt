{{- if .Values.enabled }}
MyAccount has been deployed!

Chart: {{ .Chart.Name }}-{{ .Chart.Version }}
Release: {{ .Release.Name }}
Namespace: {{ include "myaccount.namespace" . }}

Deployment Information:
  - Replicas: {{ .Values.replicaCount }}
  - Image: {{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}
  - Service: {{ include "myaccount.fullname" . }}:{{ .Values.service.port }}

Access Information:
{{- if .Values.gateway.enabled }}
  MyAccount App: https://{{ include "myaccount.gateway.hostname" . }}

  Open in browser:
  open https://{{ include "myaccount.gateway.hostname" . }}
{{- else }}
  Gateway is disabled. Use port-forward to access:
  kubectl port-forward -n {{ include "myaccount.namespace" . }} svc/{{ include "myaccount.fullname" . }} 8080:{{ .Values.service.port }}
  # Open http://localhost:8080
{{- end }}

Next Steps:
  1. Verify pods are running:
     kubectl get pods -n {{ include "myaccount.namespace" . }} -l app.kubernetes.io/name={{ include "myaccount.name" . }}

  2. Check logs:
     kubectl logs -n {{ include "myaccount.namespace" . }} -l app.kubernetes.io/name={{ include "myaccount.name" . }} --tail=50

  3. Test frontend:
     curl https://{{ include "myaccount.gateway.hostname" . }}

{{- else }}
MyAccount is disabled (enabled: false)
{{- end }}
