{{- if and .Values.enabled .Values.podDisruptionBudget.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "myaccount.fullname" . }}
  namespace: {{ include "myaccount.namespace" . }}
  labels:
    {{- include "myaccount.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.podDisruptionBudget.minAvailable }}
  selector:
    matchLabels:
      {{- include "myaccount.selectorLabels" . | nindent 6 }}
{{- end }}
